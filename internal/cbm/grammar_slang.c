// Vendored tree-sitter grammar: slang
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/slang/parser.c"
#include "vendored/grammars/slang/scanner.c"
