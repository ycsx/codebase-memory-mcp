// Vendored tree-sitter grammar: typescript
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/typescript/parser.c"
#include "vendored/grammars/typescript/scanner.c"
