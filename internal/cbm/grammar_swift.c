// Vendored tree-sitter grammar: swift
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/swift/parser.c"
#include "vendored/grammars/swift/scanner.c"
