// Vendored tree-sitter grammar: templ
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/templ/parser.c"
#include "vendored/grammars/templ/scanner.c"
