// Vendored tree-sitter grammar: typst
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/typst/parser.c"
#include "vendored/grammars/typst/scanner.c"
