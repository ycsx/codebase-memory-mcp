// Vendored tree-sitter grammar: purescript
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/purescript/parser.c"
#include "vendored/grammars/purescript/scanner.c"
