// Vendored tree-sitter grammar: svelte
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/svelte/parser.c"
#include "vendored/grammars/svelte/scanner.c"
